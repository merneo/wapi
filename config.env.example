# =============================================================================
# WAPI (WEDOS API) Configuration
# =============================================================================

# API Endpoint
# For JSON communication, /wapi/json is required (not /wapi/v1)
WAPI_BASE_URL="https://api.wedos.com/wapi/json"

# Credentials
# WAPI_USERNAME: Your login email for the administration panel
WAPI_USERNAME="YOUR_EMAIL@DOMAIN.TLD"

# WAPI_PASSWORD: Password specifically for the WAPI interface
# NOTE: This is NOT your customer account password (unless you set them to be the same).
# This password is set in the administration panel under the WAPI section.
WAPI_PASSWORD="YOUR_WAPI_PASSWORD"

# Optional: Force IPv4 connections (useful when IPv6 is not whitelisted in WEDOS)
# Set to "true", "1", "yes", or "on" to force IPv4-only connections
# WAPI_FORCE_IPV4=false